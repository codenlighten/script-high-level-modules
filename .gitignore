node_modules/
package-lock.json
*.log

# Real keys. Never committed.
.wallet.json
.wallet.utxos.json

# A spend refused after its coins were funded, kept for a retry.
.pending-spend-*.hex

# scriptmin results for SCRIPTMIN=1 builds (src/minimize.js).
.scriptmin-cache/
